version: 1.0
runtime: docker
build:
  commands:
    build:
      - docker build -t rust-messaging-app .
run:
  runtime-version: latest
  command: ./rust-messaging-app
  network:
    port: 8080
    env: PORT
  env:
    - name: RUST_LOG
      value: "info"
